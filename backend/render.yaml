services:
  - type: web
    name: asap-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
